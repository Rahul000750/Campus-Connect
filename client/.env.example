# Optional. If unset in dev, the client uses /api (Vite proxies to the API on port 5174).
# Set this for production or if you prefer calling the API host directly in dev.
# VITE_API_URL=${import.meta.env.VITE_API_URL}
